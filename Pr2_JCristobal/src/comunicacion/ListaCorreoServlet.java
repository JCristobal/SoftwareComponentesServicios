package comunicacion;

import java.io.IOException;

import javax.servlet.ServletException;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

public class ListaCorreoServlet {

	
	protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException ,IOException { 
/*
		String accion = request.getParameter("action");
				if(accion.equals("listarUsuarios")){
					Lista<usuario> listaUsuarios = DDVusuario.listaUsuarios();
					ObjectOutputStream objeto = new ObjetoOuputStream(response.getOutputStream());
					objeto.writeObject(listaUsuarios);
					objeto.flush();
					objeto.close();
				}

*/

	}
	
	
}
